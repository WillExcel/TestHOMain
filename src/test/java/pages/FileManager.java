package pages;


// In this session, you can add as much as possible file name and extension you need to use for testing purpose.
public interface FileManager {
    String nameOfFile = "ATestData";
    String csvfileExtension = ".csv";
}
